version: '3'
services:
  test_server:
    build:
      context: https://github.com/3kybika/android-serv.git#:backend_servers
    restart: unless-stopped
    container_name: test_server
    ports:
      - 80:8081